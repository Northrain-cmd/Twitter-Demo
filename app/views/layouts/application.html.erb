<!DOCTYPE html>
<html>
  <head>
    <title>TwitterDemo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/b14cc21095.js" crossorigin="anonymous"></script>
  </head>

  <body>
    <div class="notifications">
      <% flash.each do |type, message| %>
      <p class="flash alert mb-0 <%= flash_class(type) %>"><%= message %></p>
    <% end %>
    </div>
     <nav class="navbar bg-info ">
          <%= link_to "Twitter", root_path, class: "text-white nav-link nav-item text-decoration-none me-auto fs-1" %>
          <%= link_to "Add a tweet", root_path, class: "btn btn-primary text-decoration-none ms-auto me-3" %>
          <div class="controls">
            <% if user_signed_in? %>
             <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-danger text-decoration-none mx-4" %>
             <%= link_to current_user.name, edit_user_registration_path, class: "btn btn-warning text-decoration-none mx-4" %>
            <% else %>
             <%= link_to "Login", new_user_session_path, class: "btn btn-primary text-decoration-none mx-4" %>
             <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-success text-decoration-none mx-4" %>
            <% end %>
          </div>
      </nav>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
